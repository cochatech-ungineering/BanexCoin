name: banexcoin
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^8.1.0
  file_picker: ^8.1.2
  csv: ^6.0.0
  excel: ^4.0.6
  path_provider: ^2.1.5
  dio: ^5.8.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
